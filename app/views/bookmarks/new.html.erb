<h1 class='text-center mt-3'>Add a movie to <%= @list.name %></h1>
<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit 'Save' %>
<% end %>
